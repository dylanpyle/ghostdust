#!/bin/bash

function loadenv() {
  set -o allexport
  eval $($@ | xargs -d '\n')
  set +o allexport
}

function loadenv_local() {
  loadenv "cat .env"
}

function loadenv_test() {
  loadenv_local
  loadenv "cat .test.env"
}
